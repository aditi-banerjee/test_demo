<%= form_with(model: book_product, local: true) do |form| %>
  <% if book_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_product.errors.count, "error") %> prohibited this book_product from being saved:</h2>

      <ul>
      <% book_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :buyer_id %>
    <%= form.select(:buyer_id, options_for_select(option_for_buyer)) %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.select(:product_id, options_for_select(option_for_product)) %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
